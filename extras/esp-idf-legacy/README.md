# Legacy ESP-IDF component

This directory preserves the original ESP-IDF project layout that preceded the
Arduino-library conversion. It is not used by Arduino IDE or Arduino CLI.

For new ESP32 Arduino sketches, use the library in the repository root:
`src/RadiationD.h` and the examples in `examples/`.
